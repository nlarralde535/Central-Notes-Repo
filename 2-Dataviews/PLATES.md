# Priority A
```dataview
TABLE priority
FROM #Plates/Plate and -#Plates/Template
WHERE priority = "A"
```

---

# Priority B
```dataview
TABLE priority
FROM #Plates/Plate and -#Plates/Template
WHERE priority = "B"
```

---

# Priority C
```dataview
TABLE priority
FROM #Plates/Plate and -#Plates/Template
WHERE priority = "C"
```

---

# Priority D
```dataview
TABLE priority
FROM #Plates/Plate and -#Plates/Template
WHERE priority = "D"
```

---

# Priority null
```dataview
TABLE priority
FROM #Plates/Plate and -#Plates/Template
WHERE priority = "null"
```
